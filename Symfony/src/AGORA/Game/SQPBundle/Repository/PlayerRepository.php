<?php

namespace AGORA\Game\SQPBundle\Repository;

/**
 * PlayerRepository
 *
 * This class was generated by the Doctrine ORM. Add your own custom
 * repository methods below.
 */
class PlayerRepository extends \Doctrine\ORM\EntityRepository {
    //Retourne tous les joueurs d'une partie
    public function getAllPlayersFromLobby($idGame) {
        return $this->createQueryBuilder('a')
            ->join('a.idGame', 'g')
            ->join('a.idUser', 'u')
            ->where("g.id = :id")
            ->setParameter('id', $idGame)
            ->getQuery()->getResult();

    }

    public function nbPresentPlayersFromLobby($idGame) {
        return $this->createQueryBuilder('a')
            ->select('COUNT(a)')
            ->join('a.idGame', 'g')
            ->join('a.idUser', 'u')
            ->where("g.id = :id")
            ->setParameter('id', $idGame)
            ->getQuery()->getResult();
    }
}
